spring:
    config:
        import: optional:file:.env[.properties]
    application:
        name: sarana-resturant-system
    datasource:
      url:  jdbc:postgresql://localhost:5432/db-resturant
      username: ${DB_USERNAME}
      password: ${DB_PASSWORD}
      driver-class-name: org.postgresql.Driver
      Jackson:
        date-format: yyyy-MM-dd HH:mm:ss
        time-zone: Asia/Phnom_Penh
    jpa:
        hibernate:
          ddl-auto: update
        properties:
          hibernate:
            dialect: org.hibernate.dialect.PostgreSQLDialect
          show-sql: true
springdoc:
  api-docs:
    enabled: true
Cloudinary:
  cloud_name: "df5hvjteu"
  api_key: "336348351514853"
  api_secret: "N7PJTOGS1zg7R_-JyeLHm8752rI"
